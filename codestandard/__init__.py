"""Code-standard engine: measure source against the house standard.

The evaluative arm of Theory's *"commit nothing you could not explain"* — a
CLI gate (``python -m codestandard <project_root>``) that measures source
against the five ratified principles in the packaged house standard
(``code_standard.yaml``) and returns pass/fail.
Spec: standards/codestandard/SPEC.md.
"""

__version__ = "0.0.1"
